import { LookupAdminBlock, INGREDIENT_CATEGORY_CONFIG } from "core-lib";

const IngredientCategoriesSettingsPage = () => {
  return <LookupAdminBlock config={INGREDIENT_CATEGORY_CONFIG} />;
};

export default IngredientCategoriesSettingsPage;
